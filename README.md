# Ledger

A personal nutrition + strength tracker for vegetarian powerlifters.

## Setup

1. `npm install`
2. Add your Anthropic API key to `.env` as `ANTHROPIC_API_KEY=sk-ant-...`
3. `npm run dev` for local development
4. Deploy to Vercel for production
